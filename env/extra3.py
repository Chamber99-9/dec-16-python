"""Given a list of integers,
write logic (without using loops) to check if:
the list has at least 6 elements
the maximum value is divisible by 3
the minimum value is greater than 10
Display "Valid List" if all conditions are satisfied."""

"""validity={
    "list":[100,200,300,400,500,600,700,800,900]
}
if ((len(validity['list']))>=6 and max(validity['list'])%3==0 and min(validity['list'])>=10):
    print("Valid List")
else:
    print("Not Valid List")

"""
